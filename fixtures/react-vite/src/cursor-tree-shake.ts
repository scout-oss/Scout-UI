import { StickerCursor } from "@scout-ui/react";

// Packed broad-barrel probe: Cursor should retain its own engine without
// pulling Peel, Stack, Navbar/Radix, Trail, or the official sticker pack.
export const PackedCursor = StickerCursor;
