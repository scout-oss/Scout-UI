import { StickerNavbar } from "@scout-ui/react/sticker-navbar";

// The packed harness builds this entry separately to prove that a real Navbar
// consumer retains both the component and its Radix Dialog implementation.
export const PackedNavbar = StickerNavbar;
