import { StickerStack } from "@scout-ui/react";

// Packed broad-barrel probe: Stack should retain its bounded deck engine
// without pulling unrelated interaction leaves or the official sticker pack.
export const PackedStack = StickerStack;
