import { StickerTrail, useStickerTrail } from "@scout-ui/sticker-trail";

// Standalone probe used separately from the full browser fixture so its
// package-level bundle cost and dependency boundary remain measurable.
export { StickerTrail, useStickerTrail };
