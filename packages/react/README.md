# @scout-ui/react

React entry package for Scout UI. Milestone 2 contains packaging and
server/client-boundary sentinels only; production components are implemented in
their later milestones.

The package supports the root import plus explicit public subpaths. Import
`@scout-ui/react/styles.css` for the public stylesheet entry.
