// The production Sticker API is implemented in Milestone 5. This serializable
// sentinel validates the final server-compatible subpath before that work.
export const stickerEntryStatus = "server-compatible" as const;
